# Dependencies & build output
node_modules
dist
coverage/
*.tsbuildinfo

# Env & secrets — never commit
.env
.env.*

# Logs & dumps
*.log
*.orig
*.rej

# Local AI-agent context dumps (transcripts/logs) — never commit
contextfromantigravitycli/

# Reference repos & working notes — never commit
friendsrepo/
docs/

# Analysis reports (bug hunts, audits) — private working files, not product code.
draft/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local
.cache/
