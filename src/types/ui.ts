/**
 * UI domain types — re-exported from messaging for a single source of truth.
 * Split from src/types/index.ts (Stage 1).
 */
export type { ToastOptions, ProgressPayload, RangeSelection, RegionSelection } from "../messaging/events";
