/**
 * Editor flight recorder — REMOVED.
 * All console.debug/log diagnostics were stripped (they cost cycles and
 * console noise for zero user value). console.warn/error remain at real
 * failure sites. This module is kept as an empty placeholder so old
 * imports fail loudly at build time instead of resolving to stale code.
 */
export {};
